# Define the list
L = ['Network', 'Bio', 'Programing', 'Physics', 'Music']
# Loop through each item in the list
for item in L:
    # Check if the length of the items is greater than 0 and if it starts with the letter "B"
    if len(item) > 0 and item.startswith('B'):
        print(item)
